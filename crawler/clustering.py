# todo:
def select_first_mean_points(k):
    first_points = []
    return first_points


# todo:
def compute_best_k(k_upper_bound):
    return k_upper_bound


def k_means(k, theta):
    first_mean_points = select_first_mean_points(k)
    j= compute_j()
    for doc_id

